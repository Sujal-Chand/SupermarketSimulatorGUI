<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
    "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <property name="hibernate.connection.driver_class">org.apache.derby.jdbc.EmbeddedDriver</property>
        <property name="hibernate.connection.url">jdbc:derby:database/SupermarketDB;create=true</property>
        <property name="hibernate.connection.username"></property>
        <property name="hibernate.connection.password"></property>

        <property name="hibernate.dialect">org.hibernate.dialect.DerbyDialect</property>

        <property name="hibernate.show_sql">false</property>
        <property name="hibernate.format_sql">false</property>

        <!--
            Database schema auto-creation/update.
            'update' will create tables if they don't exist, and update if they change.
            'create' will drop and re-create the schema on every run (useful for testing).
            'validate' will validate the schema, but not modify it.
            'none' will do nothing.
        -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <mapping class="com.pdcgame.Models.GameSave"/>

    </session-factory>
</hibernate-configuration>
